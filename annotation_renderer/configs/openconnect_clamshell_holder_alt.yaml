$schema: ../schemas/annotation-render-config.schema.json
extends: openconnect_clamshell_holder_default.yaml
variant_name: openconnect_clamshell_holder_alt
default_variant: openconnect_clamshell_holder_alt

# Compatibility entry point. Add new image variations to the canonical config instead.
variants:
- name: openconnect_clamshell_holder_alt
  extends_variant: alt
  job_name: clamshell_holder_scene_default
