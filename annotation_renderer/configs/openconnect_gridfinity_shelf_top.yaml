$schema: ../schemas/annotation-render-config.schema.json
extends: openconnect_gridfinity_shelf_default.yaml
variant_name: openconnect_gridfinity_shelf_top
default_variant: openconnect_gridfinity_shelf_top

# Compatibility entry point. Add new image variations to the canonical config instead.
variants:
- name: openconnect_gridfinity_shelf_top
  extends_variant: top
  job_name: gridfinity_shelf_scene_default
