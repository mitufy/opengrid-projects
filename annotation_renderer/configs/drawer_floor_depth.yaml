$schema: ../schemas/annotation-render-config.schema.json
extends: drawer_container_empty.yaml
variant_name: drawer_floor_depth
default_variant: drawer_floor_depth

# Compatibility entry point. Add new image variations to the canonical config instead.
variants:
- name: drawer_floor_depth
  extends_variant: floor_depth
  job_name: opengrid_drawer_floor
