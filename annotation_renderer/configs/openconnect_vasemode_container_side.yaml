$schema: ../schemas/annotation-render-config.schema.json
extends: openconnect_vasemode_container_default.yaml
variant_name: openconnect_vasemode_container_side
default_variant: openconnect_vasemode_container_side

# Compatibility entry point. Add new image variations to the canonical config instead.
variants:
- name: openconnect_vasemode_container_side
  extends_variant: side
  job_name: vasemode_container_scene_default
